/*
    The goal is to write three following functions

    guard - the opposite of if
    until - the opposite of while
    forceRun - whatever happen inside (exception), just continue
 */

fun guard() {}
fun until() {}
fun forceRun() {}